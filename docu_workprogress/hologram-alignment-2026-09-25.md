# Hologram alignment and consistent activation

User review of `Zu_korrigieren/1.png` supersedes the earlier section-switching concept.

- Shared physical height: 9.871669316375199 world units, common vertical placement and synchronized floating. Both pedestal jets terminate at the corresponding page edge. The particle taper reaches further into the edge transition.
- Pedestal labels now contain only text, with a transparent canvas background.
- Activation preserves bloom, background lighting, frame opacity and document textures. Desktop project controls are transparent accessible links following the real scene sheets; they no longer replace the canvas appearance with a different HTML design.
- Jana/Tiefgang is the only website listed. Recovery Lab remains on the second wing. Both open directly. No separator, diamond, ring toggle or section-selection click/keyboard/swipe remains. Pinch and arrow zoom, ESC and the projection flight remain.
- Mobile / no-WebGL views expose both documents without switching.
- Centred headings in project sheets, IHK hologram pages and HTML readers. CV web assets are generated from the existing source with vector masks replacing headings and removing the outer decorative border. Original SVGs and downloadable documents are retained. `build:cv-hologram` regenerates these assets.

Verification: `test:hologram-alignment` checks all three physical heights and vertical positions, transparent label backgrounds, byte-identical preview canvas across activation, absence of switching controls and direct opening of both contents, including mobile. DE/EN image assets generated; production build and browser checks performed locally. No push/deployment.
